<form method="post" action="{{ path('app_sessions_exercices_delete', {'id': sessions_exercice.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sessions_exercice.id) }}">
    <button class="btn">Delete</button>
</form>
